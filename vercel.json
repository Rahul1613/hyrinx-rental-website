{
  "buildCommand": "prisma generate && next build",
  "devCommand": "next dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["hkg1"],
  "env": {
    "DATABASE_URL": {
      "description": "PostgreSQL database connection URL (pooled connection via PgBouncer)"
    },
    "DIRECT_URL": {
      "description": "Direct PostgreSQL connection URL for migrations (port 5432)"
    }
  }
}
